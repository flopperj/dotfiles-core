# Forbidden tokens for dotfiles-core leakage check.
# Each line (non-comment) is matched case-insensitively with word-boundary
# anchors against all files in the repo (excluding .git/ and the test fixture).
#
# Word-boundary definition: [^a-zA-Z0-9_-] on each side (BSD grep-portable).
# This means:
#   - "intuit" matches but "intuitive" / "intuition" do NOT.
#   - "@intuit.com" matches because "@" is not in [a-zA-Z0-9_-].
#   - "build.intuit.com" matches because "." is not in [a-zA-Z0-9_-].
#   - "DAST-Orch" matches because "-" IS in [a-zA-Z0-9_-], but the full
#     token "DAST-Orch" contains "-" internally — the match anchors on the
#     surrounding characters, not the internal ones.

mailchimp
intuit
turbotax
quickbooks
cg-tax
build.intuit.com
@intuit.com
@mailchimp.com
DAST-Orch
