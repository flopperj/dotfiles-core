# Forbidden tokens for dotfiles-core leakage check.
# Each line (non-comment) is matched case-insensitively with word-boundary
# anchors against all files in the repo (excluding .git/ and the test fixture).
#
# Word-boundary definition: [^a-zA-Z0-9] on each side (alphanumeric only).
# Underscore (_) and hyphen (-) are treated as non-word chars, so:
#   - "intuit" matches but "intuitive" / "intuition" do NOT.
#   - "intuit_client_secrets" DOES match (underscore is not a word char).
#   - "your-intuit-username" DOES match (hyphen is not a word char).
#   - "@intuit.com" matches because "@" is not alphanumeric.
#   - "build.intuit.com" matches because "." is not alphanumeric.

mailchimp
intuit
turbotax
quickbooks
cg-tax
build.intuit.com
@intuit.com
@mailchimp.com
DAST-Orch
